export interface INewBook {
  title: string;
  description: string;
  numberOfPages: number;
  language: string;
  imageUrl: string;
  buyUrl: String;
}
export interface IBook {
  _id: String;
  languageText: string;
}
